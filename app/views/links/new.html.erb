<%= form_for @link, class: "form-horizontal" do |l| %>
  <div class="form-group form-group-lg">
    <div class="row">
      <div class="col-md-8">
        <%= l.text_field :actual_url, class: "form-control", placeholder: "Enter a URL to shorten" %>
      </div>
      <div class="col-md-3">
        <%= l.submit "Shorten", class: "btn btn-primary btn-lg" %>
      </div>
    </div>
  </div>
<% end %>